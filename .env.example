# Vite envs (frontend)
VITE_SUPABASE_URL="https://YOUR-PROJECT.supabase.co"
VITE_SUPABASE_ANON_KEY="YOUR-ANON-KEY"

# Supabase Edge Function secret (already set via Supabase Secrets for the function runtime)
# OPENAI_API_KEY is stored server-side via: supabase secrets set OPENAI_API_KEY=sk-…